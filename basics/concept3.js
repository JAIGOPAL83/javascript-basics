// Template Literals
// Template literals allow you to embed expressions and variables directly within a string using backticks (`) and ${} syntax.

const name = "Jaigopal";
const city = "Melbourne";

console.log(`${name} lives in ${city}`);
